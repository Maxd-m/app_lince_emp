import 'package:flutter/material.dart';

/// Esta clase define una paleta de colores para la aplicación, utilizando constantes para cada color.

class AppColors {
/* Nota: Static no necesita ser instanciada para ser utilizada, es decir,
puedes acceder a estos colores directamente a través de la clase AppColors
 sin tener que crear un objeto de esa clase.
 const se utiliza para definir constantes en tiempo de compilación,
 lo que significa que estos valores no pueden cambiar en tiempo de ejecución.
 */
  static const sidebarBackground = Color(0xFF111827);
  static const textPrimary = Color(0xFF526581);
  static const textSecondary = Color(0xFF8491a5);
  static const bgPrimary = Color(0xFFf3f4f6);
  static const iconSidebar = Color(0xFFe5e7eb);
  static const borderTextField = Color(0xFFD4DAE1);
  static const selectedWidegets = Color(0xFF696cff);
  static const spaceholder = Color(0xFF3A3A3A);
}